spring:
  application:
   name: booking-app-spring

  datasource:
    url: jdbc:postgresql://localhost:5433/postgres
    username: myuser
    password: 12345
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
